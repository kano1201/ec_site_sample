<h1>News一覧画面</h1>
<% @newss.each do |news| %>

<div>
  <p>Day:<%= news.created_at.strftime('%Y/%m/%d') %></p>
  <p>Title:<%= link_to newss_path(news.id) do %><%= news.news_title %><%end%></p> <!--投稿の中身が表示されない（bodyも同様）-->
  <p>Body: <%= news.news_body %></p>
</div>
<% end %>
<%= paginate @newss %>